import { connect } from "react-redux";
import Supplier_Journal from "../Components/journals/supplier_journal";
import { Settablehistory } from "../Service/Actions/action";

const dispatchpart = (dispatch) => ({
  Settable_history: (data) => dispatch(Settablehistory(data)),
});

const getpart = (state) => ({
  state,
});

export default connect(getpart, dispatchpart)(Supplier_Journal);
